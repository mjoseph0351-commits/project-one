/* Topbar Styles */
.topbar {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 56px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    background: rgba(0, 121, 107, 0.85);
    z-index: 1100;
    box-shadow: 0 2px 16px rgba(0,0,0,0.12);
    transition: all 0.3s ease;
}

.topbar-logo {
    height: 40px;
    width: auto;
    margin-left: 18px;
    margin-right: 24px;
    border-radius: 6px;
    vertical-align: middle;
    box-shadow: 0 1px 4px rgba(0,0,0,0.07);
}

.topbar-title {
    font-size: 1.35rem;
    font-weight: 700;
    color: #fff;
    letter-spacing: 0.5px;
    white-space: nowrap;
    vertical-align: middle;
    display: inline-block;
    text-align: center;
    line-height: 1.5;
    margin: 16px 0;
}

.topbar-links {
    list-style: none;
    display: flex;
    gap: 40px;
    margin: 0 auto;
    justify-content: center;
    width: 100%;
    padding: 0;
}

/* Adjustments for topbar-link to fit comfortably */
.topbar-link a {
    color: #fff;
    text-decoration: none;
    font-size: 1.1rem;
    font-weight: 500;
    padding: 8px 16px;
    border-radius: 6px;
    transition: background 0.2s, color 0.2s;
    white-space: nowrap; /* Prevent text wrapping */
    overflow: hidden; /* Hide overflow content */
    text-overflow: ellipsis; /* Add ellipsis for overflow text */
}

/* Ensure spacing on the right side */
.topbar-links {
    margin-right: 16px; /* Add allowance on the right side */
}

/* Adjustments for smaller screens */
@media (max-width: 768px) {
    .topbar {
        flex-direction: column;
        height: auto;
        padding: 8px;
    }

    .topbar-title {
        font-size: 1.2rem;
        margin-bottom: 8px;
        display: none !important;
    }

    .topbar-links {
        flex-direction: column;
        gap: 16px;
        margin-right: 16px; /* Maintain right-side allowance */
        padding: 8px;
        border-radius: 8px;
    }
}

/* Adjustments for tablets */
@media (min-width: 769px) and (max-width: 1024px) {
    .topbar-links {
        gap: 24px;
    }
}

.topbar-link a:hover {
    background: rgba(0,0,0,0.10);
    color: #fff;
    box-shadow: 0 2px 12px 0 rgba(0,0,0,0.25);
    border-bottom: 3px solid #000;
}

.topbar-link a.active {
  background: rgba(0,0,0,0.10);
  color: #fff;
  box-shadow: 0 2px 12px 0 rgba(0,0,0,0.25);
  border-bottom: 3px solid #000;
}

/* Adjust topbar-links for smaller screens */
@media (max-width: 768px) {
    .topbar-links {
        flex-direction: column; /* Stack links vertically */
        gap: 16px; /* Add spacing between links */
        margin-right: 16px; /* Add allowance on the right side */
        padding: 8px; /* Add padding for comfortable spacing */
        border-radius: 8px; /* Add a slight radius for aesthetics */
    }
}

/* Drop down */
/* Container for dropdown */
.topbar-link-dropdown {
  position: relative;
  display: inline-block;
}

/* Dropdown toggle link */
.topbar-link-dropdown > a {
  color: #fff;
  text-decoration: none;
  font-size: 1.1rem;
  font-weight: 500;
  padding: 8px 16px;
  border-radius: 6px;
  transition: background 0.2s, color 0.2s;
}

/* Hover effect for main link */
.topbar-link-dropdown > a:hover {
  background: rgba(0,0,0,0.10);
  box-shadow: 0 2px 12px rgba(0,0,0,0.25);
  border-bottom: 3px solid #000;
}

@media (max-width: 768px) {
    .topbar {
        flex-direction: column;
        height: auto;
        padding: 8px;
    }
    .topbar-title {
        font-size: 1.2rem;
        margin-bottom: 8px;
    }
    .topbar-links {
        flex-direction: column;
        gap: 16px;
        margin-right: 16px; /* Maintain right-side allowance */
        padding: 8px;
        border-radius: 8px;
    }

    /* Replace topbar-title with logo on smaller screens */
    .topbar-title {
        display: none !important; /* Hide the title */
    }

    .topbar-logo {
        display: block !important; /* Ensure the logo is visible */
        height: 40px; /* Adjust logo size */
        width: auto;
        margin: 0 auto; /* Center the logo */
    }
}

/* For screens between 768px and 1024px (tablets) */
@media (min-width: 769px) and (max-width: 1024px) {
    .topbar-links {
        gap: 24px;
    }
    .topbar-link:hover .dropdown {
    display: block;
    opacity: 1;
    transform: translateY(0);
}

/* Accessibility improvements */
.topbar-link a {
    outline: none;
}

.topbar-link a:focus + .dropdown,
.dropdown a:focus {
    display: block;
    opacity: 1;
    transform: translateY(0);
}

/* Show dropdown on hover */
.topbar-link-dropdown:hover .dropdown-menu {
  display: flex; /* Make the dropdown visible */
  opacity: 1; /* Ensure full visibility */
  transform: translateY(0); /* Reset any transform effects */
  transition: opacity 0.3s ease, transform 0.3s ease; /* Smooth transition */
}

/* Topbar Styles */
.topbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 10px 20px;
    background-color: rgba(0, 121, 107, 0.85);
    border-bottom: 1px solid #ccc;
}

.topbar-title {
    font-size: 1.5em;
    font-weight: bold;
}

.topbar-links {
    display: flex;
    gap: 20px;
    list-style: none;
    margin: 0;
    padding: 0;
}

.topbar-link a {
    text-decoration: none;
    color: #333;
    font-weight: 500;
}

.topbar-link a:hover {
    text-decoration: underline;
}
/* Medium screens: max-width 768px */
@media (max-width: 768px) {
  /* Topbar */
  .topbar {
    flex-direction: column;
    height: auto;
    padding: 8px;
  }

  .topbar-title {
    font-size: 1.2rem;
    margin-bottom: 8px;
  }

  .topbar-links {
    flex-direction: column;
    gap: 16px;
    margin-right: 16px; /* Maintain right-side allowance */
    padding: 8px;
    border-radius: 8px;
  }
    /* Adjust topbar-title for smaller screens */
  .topbar-title {
    display: flex;
    flex-direction: column; /* Stack words vertically */
    align-items: center; /* Center-align the text */
    justify-content: center;
    text-align: center; /* Ensure text is centered */
    font-size: 1rem; /* Adjust font size for readability */
    line-height: 1.5; /* Add spacing between words */
    margin: 16px 0; /* Add spacing around the title */
    word-wrap: break-word; /* Allow breaking into manageable words */
    overflow-wrap: break-word; /* Ensure long words break properly */
  }

  .topbar-title span {
    display: block; /* Ensure each word is on its own line */
  }

  /* Ensure topbar-title is hidden on smaller screens */
  .topbar-title {
    display: none !important; /* Force hiding the title */
  }

  .topbar-logo {
    display: block !important; /* Ensure the logo is visible */
    height: 40px; /* Adjust logo size */
    width: auto;
    margin: 0 auto; /* Center the logo */
  }
}

/* Large tablets: 769px to 1024px */
@media (min-width: 769px) and (max-width: 1024px) {
  .topbar-links {
    gap: 24px;
  }
  @media (max-width: 768px) {
    .topbar {
        position: fixed;
        top: 0;
        right: 0; /* Ensure it stays on the right side */
        left: unset; /* Remove any left positioning */
        width: 56px; /* Sidebar width */
        height: 100%; /* Full height for sidebar */
        flex-direction: column; /* Stack items vertically */
        align-items: center; /* Center items horizontally */
        justify-content: flex-start; /* Align items to the top */
        padding-top: 16px; /* Add spacing at the top */
    }

    .topbar-title {
        writing-mode: vertical-rl; /* Rotate text vertically */
        transform: rotate(180deg); /* Adjust text orientation */
        margin-bottom: 16px;
        word-wrap: break-word; /* Allow breaking into manageable words */
        overflow-wrap: break-word; /* Ensure long words break properly */
        text-align: center; /* Center-align the title */
        font-size: 1rem; /* Adjust font size for readability */
    }

    .topbar-title {
        display: flex;
        flex-direction: column; /* Stack words vertically */
        align-items: center; /* Center-align the text */
        justify-content: center;
        text-align: center; /* Ensure text is centered */
        font-size: 1rem; /* Adjust font size for readability */
        line-height: 1.5; /* Add spacing between words */
        margin: 16px 0; /* Add spacing around the title */
        writing-mode: unset; /* Remove rotation */
        transform: none; /* Reset any transformations */
    }

    .topbar-title span {
        display: block; /* Ensure each word is on its own line */
    }

    .topbar-links {
        flex-direction: column; /* Stack links vertically */
        gap: 16px;
    }
}
